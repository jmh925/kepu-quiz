# 运行期依赖（Python 3.10+）
# 安装：pip install -r requirements.txt
# 说明：仓库里**没有**内置依赖副本，请先执行上面的 pip 命令。
#       （scripts/run_server.cmd 里留了一行"如果存在 backend/deps 就把它加进
#         PYTHONPATH"的兼容逻辑，那是给离线环境自己拷依赖用的，仓库默认不带。）
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
pydantic>=2.5
httpx>=0.26
PyJWT>=2.8
# 知识库文档解析的可选依赖：不装则仅支持 txt / md
pypdf>=4.0
docx2txt>=0.8
# 管理端/论文图表生成脚本使用（可选）
Pillow>=10.0
