# 第 3 章 系统需求分析

## 3.1 可行性分析

### 3.1.1 技术可行性

学生端采用微信小程序原生框架，以 WXML、WXSS、JavaScript 与 JSON 组织页面，无需构建工具链；管理端采用原生 HTML、CSS 与 JavaScript，由后端以静态目录托管。后端采用 Python 3.13、FastAPI、Uvicorn 与 Pydantic v2，并可用标准库 sqlite3 持久化数据。大模型通过 DeepSeek 的 OpenAI 兼容接口接入，由 httpx 调用；检索增强使用中文 2-gram 词频向量与余弦相似度。后端已跑通全部核心链路。

### 3.1.2 经济可行性

开发工具、开源框架与 SQLite 均无授权费用。运行阶段的主要成本来自大模型调用，DeepSeek 按 Token 计费，单次闯关仅五至十五道单项选择题。系统在未配置模型密钥时会自动降级到内置科普题库，演示与验收可在零调用成本下完成；部署只需一台可运行 Python 的机器与单个数据库文件。

### 3.1.3 操作可行性

学生端以微信小程序交付，免安装，扫码或分享即可进入，界面以大字号题干与胶囊按钮降低操作门槛，低年级学生无需培训；主要业务流程不超过三步。教师与家长上传资料即可让学生基于指定材料练习；管理端为表单化操作。

## 3.2 用户角色与典型使用场景分析

### 3.2.1 学生用户

学生是核心用户，覆盖小学低年级、小学高年级与初中三个学段。典型场景为：在去闯关页选择学段并输入感兴趣的主题；进入答题页逐题作答并即时获得讲解；在复盘报告页查看掌握度、知识总结与薄弱知识点；再到错题本页查看排行并点击只练错题复习。

### 3.2.2 教师与家长用户

教师与家长为次要用户，主要通过知识库参与。教师把讲义或课外读本上传知识库，系统自动完成文本提取与分块，学生出题时选择该资料即可把练习范围限定在指定内容上。家长可引导学生围绕家庭科普读物出题，并在个人中心查看经验值与闯关记录。

### 3.2.3 系统管理员

系统管理员负责三类工作：题库资源池维护，包括题目新增、修改、删除与上下架及按主题、学段、关键词检索；用户管理，包括分页查询与账号状态启用、停用；运行数据查看，通过运行看板获取用户、闯关、答题、错题本、知识库与资源池指标，并通过操作日志追溯写操作。

## 3.3 功能需求分析

### 3.3.1 学生端功能需求

学生端划分六个功能模块。闯关出题模块接收主题与可选题量、学段、文档标识，经内容安全校验与按学段组装后出题，命中资料时做检索增强，输出题目列表与来源标识。答题闯关模块比对正确选项下标，统计正确率与薄弱知识点，结算经验值并维护错题本。复盘报告模块生成掌握度、薄弱知识点与学习建议。错题本模块提供错题列表、薄弱知识点排行、只练错题与清空四项能力。个人中心展示用户信息、经验值与错题数量，知识库支持上传资料、基于资料出题与删除资料。

### 3.3.2 管理端功能需求

管理端已实现，含运行看板、题库资源池、用户管理、闯关记录与操作日志五个功能区。运行看板汇总用户、闯关、答题、错题本与资源池指标及近若干天趋势。资源池支持分页查询、主题统计与增删改，修改时只更新请求中出现的字段；用户管理提供分页查询与状态调整；闯关记录按学段与来源分页查询；操作日志记录登录与各类写操作。全部接口通过 X-Admin-Token 鉴权。功能需求汇总如表 3-1 所示。

表 3-1 功能需求汇总表

| 模块 | 功能点 | 输入 | 处理 | 输出 |
| --- | --- | --- | --- | --- |
| 闯关出题 | 主题出题 | 主题、题量、学段、文档标识 | 内容安全校验 → 文档检索 → 按学段组装请求 → 模型或题库降级 | 题目列表、来源标识、命中片段数 |
| 答题闯关 | 判题与结算 | 闯关标识、答案序列、答题耗时 | 比对下标 → 统计正确率与薄弱点 → 结算经验值 → 维护错题本 | 正确率、经验值、薄弱知识点、逐题明细 |
| 复盘报告 | 报告生成 | 闯关标识 | 读取答题记录 → 模型生成或按正确率分档套用规则模板 | 掌握度、薄弱知识点、知识总结、学习建议 |
| 错题本 | 错题管理 | 用户身份 | 答错入库并累加次数、答对移除、按错误次数排序 | 错题列表、薄弱知识点排行 |
| 错题本 | 只练错题 | 题量、学段 | 由错题重组练习卷，不调用大模型 | 练习卷题目列表 |
| 个人中心 | 个人信息 | 用户身份 | 汇总用户资料、经验值、错题与历史闯关 | 用户信息、闯关记录、错题数量 |
| 知识库 | 资料管理 | 文本或文档文件 | 文本提取 → 分块 → 建立 2-gram 词频索引 | 文档标识、分块数、文档列表 |
| 知识库 | 基于资料出题 | 主题、文档标识 | 余弦相似度取前三个片段注入请求 | 基于指定资料的题目列表 |
| 管理端 | 运行看板 | 管理员令牌 | 聚合用户、闯关、答题、错题与资源池统计 | 核心指标与趋势数据 |
| 管理端 | 题库维护 | 主题、学段、题干、选项、答案、难度 | 校验后新增、修改、删除、上下架 | 题目记录与主题分布 |
| 管理端 | 用户与记录管理 | 关键词、页码、状态 | 分页查询用户与闯关记录、调整账号状态 | 用户列表、闯关记录、日志 |

## 3.4 非功能需求分析

### 3.4.1 性能需求

性能指标按出题路径分档约定。未配置大模型密钥时，出题、判题与查询类接口的响应时间均应控制在百毫秒以内。配置密钥后，出题接口耗时取决于模型推理，预期为 10 至 40 秒，因此前端必须提供等待反馈：出题期间展示分档等待台词与科普小知识轮播，并令吉祥物保持思考中状态。

### 3.4.2 可靠性与降级需求

系统采用三层降级。出题降级：未配置密钥、调用超时、返回内容无法解析为 JSON 或题目全部不合规时，自动降级到内置科普题库，按主题关键词匹配后随机抽样，并优先取用题目资源池。报告降级：生成失败或输出未通过安全检查时，改用规则模板，按正确率分档给出掌握度与建议。复习降级：只练错题基于错题本本地数据重组练习卷，不调用大模型。

### 3.4.3 易用性需求

主要业务路径的操作步骤不超过三步。界面采用天蓝主色、胶囊按钮与卡片式布局，题干字号不小于 36rpx，可点按区域不小于 88rpx。文案不使用错误、失败、排名等字眼，答错反馈使用暖橙色而非纯红色。系统对主题为空或超长、内容不合规、资料无法解析等情形给出明确的友好提示。

### 3.4.4 安全性需求

身份认证采用 JWT 机制，学生端令牌以 HS256 签发、有效期七天，管理端令牌有效期两小时，分别通过 Authorization: Bearer 与 X-Admin-Token 请求头传递；管理端口令以 PBKDF2-HMAC-SHA256 加随机盐存储，不保存明文，连续失败登录会被临时锁定。内容安全对输入做敏感词与长度校验，对模型输出逐题过滤。

## 3.5 系统用例分析

学生端识别出七个用例：查看学段选项、主题出题、答题闯关、生成复盘报告、管理错题本与只练错题、维护知识库资料、查看个人中心；管理端识别出三个用例：管理端登录与令牌校验、运行数据查看、题库与用户管理。学生用例参与者为学生，其中错题本与个人中心需要身份标识，游客可完成出题与答题。系统用例图如图 3-1 所示。

> 图 3-1 系统用例图（见 docs/figures/图3-1_系统用例图.png）

## 3.6 本章小结

本章论证了系统可行性，梳理了三类用户角色与典型使用场景，分析了学生端六个模块与管理端五个功能区的功能需求并以表 3-1 汇总，提出了性能、可靠性与降级、易用性、安全性四方面非功能需求，归纳出学生端七个用例与管理端三个用例。

---

# 第 4 章 系统设计

## 4.1 系统总体设计

### 4.1.1 总体设计思路

系统以主题输入、出题、闯关答题、复盘提升为主线组织模块。设计遵循三条原则：降低使用门槛，采用可选登录，游客可完整体验出题与答题；外部依赖可降级，大模型仅作增强而非必需；把适合中小学生落实为可校验的学段参数，涵盖题量、题干长度、用词难度与选项长度。

### 4.1.2 系统总体设计模块

系统按角色划分为学生端与管理端两大模块。学生端含闯关出题、答题闯关、复盘报告、错题本、个人中心与知识库六个模块；管理端含运行看板、题库资源池、用户管理、闯关记录与操作日志五个功能区。题目资源池由管理端维护并在出题时优先命中。系统总体设计模块如图 4-1 所示。

> 图 4-1 系统总体设计模块图（见 docs/figures/图4-1_系统总体设计模块图.png）

## 4.2 系统功能结构设计

### 4.2.1 功能结构层次

功能结构按四层组织：主菜单层区分学生端与管理端两个入口；用户登录层中，学生端提供本地账号登录并预留通过 wx.login 换取 openid 的接入点，游客可直接进入核心功能，管理端使用账号口令登录；角色层对应学生、教师与家长以及系统管理员；功能项层展开六个学生端模块与五个管理端功能区。系统功能结构如图 4-2 所示。

> 图 4-2 系统功能结构图（见 docs/figures/图4-2_系统功能结构图.png）

### 4.2.2 各功能模块说明

各模块按输入、处理、输出说明。闯关出题以主题、题量、学段、文档标识为输入，经内容安全校验、可选文档检索、按学段组装请求与模型或题库降级，输出题目列表与来源标识。答题闯关以闯关标识与答案序列为输入，比对选项下标、统计正确率与薄弱知识点、结算经验值并维护错题本，输出成绩与逐题明细。复盘报告依据答题记录输出掌握度、薄弱知识点与建议。错题本以答错入库、答对移除为处理，输出错题列表与练习卷。知识库以文本提取、分块与词频检索为处理，输出资料列表或题目。管理端输出统计数据、题目记录与操作日志。

## 4.3 系统总体架构设计

### 4.3.1 分层架构设计

系统采用前后端分离的分层架构，自下而上为数据层、应用服务层、接入层与表现层。数据层基于 SQLite，通过标准库 sqlite3 实现连接复用与线程锁串行化写操作，并以自动建表与字段迁移完成初始化。应用服务层承载登录与鉴权、出题、判题与结算、复盘报告、知识库检索、错题本维护、内容安全与学段适配等规则，且不依赖 Web 框架。接入层由学生端与管理端两组路由构成，负责参数解析、鉴权依赖注入与统一响应。表现层为学生端六个页面与管理端五个功能区页面。系统总体架构如图 4-3 所示。

> 图 4-3 系统总体架构图（见 docs/figures/图4-3_系统总体架构图.png）

### 4.3.2 前后端分离与接口约定

接口统一使用 /api/v1 前缀，响应体统一为 code、message、data 三段式，code 为 0 表示成功、非 0 表示业务错误。学生端鉴权使用 Authorization: Bearer 请求头，令牌合法时解析出用户标识，缺失或非法时以游客身份继续处理；管理端鉴权使用 X-Admin-Token 请求头。

## 4.4 数据库设计

### 4.4.1 概念结构设计

系统可抽象出用户、闯关会话、答题记录、复盘报告、知识库文档、知识库分块、错题、管理端账号、题目资源池与操作日志共十类实体。一个用户可发起多次闯关会话；一次会话对应一条答题记录与一份复盘报告，均以闯关标识为唯一关联键；错题以题干为同一性判据，与用户构成一对多联系；题目资源池由管理端维护并在出题时读取。数据库 E-R 图如图 4-4 所示。

> 图 4-4 数据库 E-R 图（见 docs/figures/图4-4_数据库E-R图.png）

### 4.4.2 逻辑结构设计

系统共建立十张数据表，字段类型以 INTEGER、TEXT 与 REAL 为主，时间字段以本地时间字符串存储。数据访问层为关键数据表建立了索引。十张表的逻辑结构如表 4-1 至表 4-10 所示。

表 4-1 用户表 users

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 用户主键 |
| openid | TEXT | NOT NULL UNIQUE | 外部唯一标识 |
| nickname | TEXT | NOT NULL DEFAULT '学习者' | 昵称 |
| avatar_url | TEXT | NOT NULL DEFAULT '' | 头像地址 |
| total_xp | INTEGER | NOT NULL DEFAULT 0 | 累计经验值 |
| status | INTEGER | NOT NULL DEFAULT 1 | 1 正常 / 0 停用 |
| grade | TEXT | NOT NULL DEFAULT 'primary_high' | 常用学段 |
| last_login_at | TEXT | 可空 | 最近登录时间 |
| created_at | TEXT | NOT NULL DEFAULT 本地时间 | 创建时间 |
| updated_at | TEXT | NOT NULL DEFAULT 本地时间 | 更新时间 |

表 4-2 闯关会话表 quiz_sessions

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 会话主键 |
| quiz_id | TEXT | NOT NULL UNIQUE | 闯关唯一标识 |
| user_id | INTEGER | 可空 | 用户标识 |
| title | TEXT | NOT NULL DEFAULT '' | 闯关标题 |
| summary | TEXT | 可空 | 会话摘要 |
| user_input | TEXT | 可空 | 输入的主题 |
| grade | TEXT | NOT NULL DEFAULT 'primary_high' | 本局学段 |
| source | TEXT | NOT NULL DEFAULT 'bank' | ai / bank / wrongbook |
| questions_json | TEXT | NOT NULL | 题目序列 JSON |
| created_at | TEXT | NOT NULL DEFAULT 本地时间 | 创建时间 |

表 4-3 答题记录表 answer_records

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 记录主键 |
| quiz_id | TEXT | NOT NULL UNIQUE | 关联闯关标识 |
| user_id | INTEGER | 可空 | 用户标识 |
| records_json | TEXT | NOT NULL | 作答明细 JSON |
| total_questions | INTEGER | NOT NULL DEFAULT 0 | 题目总数 |
| correct_count | INTEGER | NOT NULL DEFAULT 0 | 答对题数 |
| accuracy | REAL | NOT NULL DEFAULT 0 | 正确率 |
| duration_ms | INTEGER | NOT NULL DEFAULT 0 | 答题耗时 |
| created_at | TEXT | NOT NULL DEFAULT 本地时间 | 创建时间 |

表 4-4 复盘报告表 reports

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 报告主键 |
| quiz_id | TEXT | NOT NULL UNIQUE | 关联闯关标识 |
| user_id | INTEGER | 可空 | 用户标识 |
| report_json | TEXT | NOT NULL | 报告内容 JSON |
| created_at | TEXT | NOT NULL DEFAULT 本地时间 | 创建时间 |

表 4-5 知识库文档表 knowledge_docs

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 文档主键 |
| doc_id | TEXT | NOT NULL UNIQUE | 文档唯一标识 |
| user_id | INTEGER | 可空 | 上传者标识 |
| filename | TEXT | NOT NULL | 原始文件名 |
| file_type | TEXT | NOT NULL DEFAULT 'txt' | 文件扩展名 |
| size_bytes | INTEGER | NOT NULL DEFAULT 0 | 文件字节数 |
| content | TEXT | NOT NULL | 提取出的全文 |
| status | TEXT | NOT NULL DEFAULT 'ready' | 文档状态 |
| created_at | TEXT | NOT NULL DEFAULT 本地时间 | 创建时间 |

表 4-6 知识库分块表 knowledge_chunks

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 分块主键 |
| doc_id | TEXT | NOT NULL | 所属文档标识 |
| chunk_index | INTEGER | NOT NULL | 分块序号 |
| content | TEXT | NOT NULL | 分块正文，约 300 字 |

> 该表按 doc_id 建立普通索引 idx_kb_chunks_doc。

表 4-7 错题本表 wrong_questions

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 错题主键 |
| user_id | INTEGER | 可空 | 所属用户标识 |
| quiz_id | TEXT | 可空 | 最近答错闯关标识 |
| stem | TEXT | NOT NULL | 题干，同一性判据 |
| options_json | TEXT | NOT NULL DEFAULT '[]' | 选项 JSON |
| correct_answer | INTEGER | NOT NULL DEFAULT 0 | 正确选项下标 |
| user_answer | INTEGER | 可空 | 用户所选下标 |
| analysis | TEXT | DEFAULT '' | 解析 |
| knowledge_point | TEXT | DEFAULT '' | 知识点 |
| wrong_count | INTEGER | NOT NULL DEFAULT 1 | 累计答错次数 |
| last_wrong_at | TEXT | NOT NULL DEFAULT 本地时间 | 最近答错时间 |

> 该表按 user_id 建普通索引 idx_wrong_user，并按 user_id 与 stem 建唯一索引。

表 4-8 管理端账号表 admins

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 账号主键 |
| username | TEXT | NOT NULL UNIQUE | 管理端账号 |
| password_hash | TEXT | NOT NULL | 口令散列值 |
| salt | TEXT | NOT NULL | 随机盐 |
| role | TEXT | NOT NULL DEFAULT 'admin' | 角色标识 |
| created_at | TEXT | NOT NULL DEFAULT 本地时间 | 创建时间 |

表 4-9 题目资源池表 question_pool

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 题目主键 |
| theme | TEXT | NOT NULL DEFAULT '通用' | 所属主题 |
| grade | TEXT | NOT NULL DEFAULT 'primary_high' | 适用学段 |
| stem | TEXT | NOT NULL | 题干 |
| options_json | TEXT | NOT NULL | 选项 JSON |
| answer | INTEGER | NOT NULL DEFAULT 0 | 正确选项下标 |
| analysis | TEXT | NOT NULL DEFAULT '' | 解析 |
| knowledge_point | TEXT | NOT NULL DEFAULT '科普知识' | 知识点 |
| difficulty | INTEGER | NOT NULL DEFAULT 2 | 1 易 / 2 中 / 3 难 |
| enabled | INTEGER | NOT NULL DEFAULT 1 | 1 启用 / 0 停用 |
| created_at | TEXT | NOT NULL DEFAULT 本地时间 | 创建时间 |

> 该表按 theme 与 grade 建立普通索引 idx_pool_theme。

表 4-10 操作日志表 admin_logs

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 日志主键 |
| username | TEXT | NOT NULL | 操作人账号 |
| action | TEXT | NOT NULL | 操作类型 |
| detail | TEXT | NOT NULL DEFAULT '' | 操作详情 |
| created_at | TEXT | NOT NULL DEFAULT 本地时间 | 创建时间 |

## 4.5 接口设计

### 4.5.1 接口清单

系统共设计并实现 26 个接口，其中学生端 13 个、管理端 13 个。学生端接口覆盖健康检查、学段选项、出题、判题、复盘报告、登录、个人中心、知识库文档增删查与错题本的查询、练习、清空；管理端接口覆盖登录与令牌校验、运行看板、趋势、用户查询与状态调整、题目资源池增删改查与主题统计、闯关记录与操作日志。系统接口清单如表 4-11 所示。

表 4-11 系统接口清单

| 序号 | 方法 | 路径 | 入参 | 出参 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 1 | GET | /api/v1/health | — | status、llm_configured、model | 健康检查 |
| 2 | GET | /api/v1/grades | — | grades、default | 学段选项 |
| 3 | POST | /api/v1/quiz/generate | topic、count?、doc_id?、grade? | quiz_id、title、source、grade、count、hit_chunks、dropped_unsafe、dropped_long、questions | 出题，含检索增强与降级 |
| 4 | POST | /api/v1/quiz/submit | quiz_id、answers、duration_ms? | total、correct、accuracy、xp_gained、wrong_added、mastered、wrong_total、weak_points、details | 判题与结算 |
| 5 | POST | /api/v1/report/generate | quiz_id | quiz_id、total、correct、accuracy、report | 复盘报告 |
| 6 | POST | /api/v1/user/login | code?、nickname?、grade? | token、user | 本地账号登录 |
| 7 | GET | /api/v1/user/profile | 令牌 | user、sessions、wrong_count、weak_points | 个人中心 |
| 8 | POST | /api/v1/knowledge/documents | file（multipart） | doc_id、filename、chunks、file_type、size_bytes | 上传资料并分块 |
| 9 | GET | /api/v1/knowledge/documents | 令牌 | documents | 资料列表 |
| 10 | DELETE | /api/v1/knowledge/documents/{doc_id} | doc_id | deleted | 删除资料 |
| 11 | GET | /api/v1/wrong/questions | 令牌 | questions、weak_points、total | 错题本总览 |
| 12 | POST | /api/v1/wrong/practice | count?、grade? | 同接口 3 结构，source 为 wrongbook | 只练错题 |
| 13 | DELETE | /api/v1/wrong/questions | 令牌 | cleared | 清空错题本 |
| 14 | POST | /api/v1/admin/login | username、password | token、admin | 管理端登录 |
| 15 | GET | /api/v1/admin/me | X-Admin-Token | username、role | 令牌校验 |
| 16 | GET | /api/v1/admin/dashboard | X-Admin-Token | users、quizzes、answers、wrongbook、knowledge、pool、llm | 运行看板 |
| 17 | GET | /api/v1/admin/trend?days=7 | days | days、points | 闯关量趋势 |
| 18 | GET | /api/v1/admin/users | keyword?、page、size | items、total、page、size | 用户查询 |
| 19 | POST | /api/v1/admin/users/{id}/status | id、status | user_id、status | 用户状态调整 |
| 20 | GET | /api/v1/admin/questions | theme?、grade?、keyword?、page、size | items、total、page、size | 资源池查询 |
| 21 | GET | /api/v1/admin/questions/themes | X-Admin-Token | themes | 主题统计 |
| 22 | POST | /api/v1/admin/questions | theme、grade、stem、options、answer、analysis、knowledge_point、difficulty、enabled | id | 新增题目 |
| 23 | PUT | /api/v1/admin/questions/{id} | id、待更新字段 | id | 修改题目，局部更新 |
| 24 | DELETE | /api/v1/admin/questions/{id} | id | deleted | 删除题目 |
| 25 | GET | /api/v1/admin/sessions | page、size、grade?、source? | items、total、page、size | 闯关记录 |
| 26 | GET | /api/v1/admin/logs | limit | logs | 操作日志 |

### 4.5.2 统一响应与错误码设计

接口返回统一三段式响应体，业务错误设置对应 HTTP 状态码，错误码按语义分为八类。

- 0：成功，业务数据置于 data 字段。
- 4000：参数错误，含主题为空或超长、文件超限、题目字段不合法、记录不存在。
- 4001：文档解析失败，含类型不支持、读取失败、无法提取有效文本。
- 4002：内容不合规，主题命中敏感词或模型输出未通过安全检查。
- 4003：错题本为空，只练错题时没有可用题目。
- 4010：学生端未登录，访问需要身份标识的接口但未携带合法令牌。
- 4011：管理端未认证，管理端令牌缺失、过期或不合法。
- 5000：服务器内部错误，统一捕获后返回，不暴露内部实现。

其中 4001 与 4002 为专用错误码，分别用于资料上传链路与出题、报告链路，共同构成面向未成年人的内容可控性设计。

## 4.6 出题与检索增强链路设计

出题链路含七个环节。一是主题预处理：去空格、长度与敏感词校验，不通过时返回参数或内容不合规错误码。二是可选文档检索：对分块计算中文 2-gram 词频向量并归一化，与主题向量算余弦相似度，取相似度大于零的前三个片段拼成上下文。三是按学段组装请求：由学段确定题量与题干长度上限、用词难度与选项长度，限定为四选一单项选择题且只输出 JSON。四是模型调用：httpx 同步请求 DeepSeek 对话补全接口并设置超时。五是解析与归一化：去代码块标记、截取首尾花括号后解析 JSON，补齐序号、题型与知识点。六是内容与长度过滤：逐题过安全检查并丢弃超长题目，缺口由题库补齐。七是落库与返回：题目写入闯关会话表并返回来源与过滤统计。模型未配置、调用异常或题目全部被过滤时整体降级到内置题库。

## 4.7 错题本与个性化复习设计

错题本把答错题目沉淀为复习依据。数据模型以用户标识与题干共同确定错题的同一性并建唯一索引；判题时答错的题写入错题本，已存在则累加答错次数并更新所选选项与最近答错时间，答对的题视为已掌握并移除，实现答错即记录、掌握即移除的语义。错题列表按答错次数与最近答错时间降序排序；薄弱知识点按知识点聚合、对答错次数求和后给出排行。只练错题取出答错次数最高的若干道题，重新编号并补齐题型与知识点，组成练习卷写入会话表，来源标记为错题本；该路径不调用大模型。相关字段设计见表 4-7。

## 4.8 安全与降级机制设计

安全机制含五方面。其一，配置外置：模型密钥、签名密钥与数据库路径等通过环境变量注入，源代码中不出现真实密钥。其二，身份认证：学生端以 HS256 签发 JWT，有效期七天；管理端令牌有效期两小时，管理端口令以 PBKDF2-HMAC-SHA256 加随机盐存储，连续失败登录会被临时锁定。其三，可选登录：游客可完整体验出题与答题，仅错题本与个人中心要求登录，微信登录预留通过 wx.login 换取 openid 的接入点。其四，内容安全：输入侧做敏感词与长度校验，输出侧对模型生成的题目与报告逐项过滤。其五，统一异常处理：响应体收敛为统一结构并覆盖全部异常分支。三层降级为出题降级到资源池与内置题库、报告降级到规则模板、只练错题不依赖大模型。

## 4.9 界面设计

学生端共六个页面。去闯关页提供学段选择、主题输入、六类快捷主题与出题等待区，等待期间轮播分档台词；答题闯关页展示进度条、题干、四个选项与即时判分，答错以暖橙色标注并给出解析；复盘报告页呈现掌握度、三句话知识总结、薄弱知识点与建议，并标注报告来源；错题本页展示错题列表、薄弱知识点排行与只练错题入口；知识库页支持上传讲义、列出资料、基于资料出题与删除；个人中心页展示经验值、错题数量与历史闯关。视觉上以天蓝为主色，采用胶囊按钮与圆角卡片，题干不小于 36rpx，可点按区域不小于 88rpx，不使用错误、失败、排名等字眼；吉祥物由纯 CSS 绘制，含待机、思考中、答对、差一点点、鼓励与通关六种状态。管理端为功能区切换的单页面布局，共五个功能区。

## 4.10 本章小结

本章明确了以主题输入、出题、闯关答题、复盘提升为主线的设计思路；按主菜单、用户登录、角色、功能项四层描述了功能结构；设计了四层架构与前后端分离的接口约定；给出十张表的逻辑结构；给出 26 个接口清单与错误码约定；并设计了出题与检索增强链路、错题本与个性化复习机制、安全与三层降级机制。

<!-- 第3章 2283 字（正文 1794 + 表 3-1 489）；第4章 3537 字（正文 2875 + 表 4-1～表 4-11 662） -->
