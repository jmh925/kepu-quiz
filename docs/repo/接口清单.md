# 接口清单（由源码自动生成）

> 本文档由 `tools/gen_docs.py` 从 FastAPI 路由表导出，
> 修改接口后请重跑脚本以保持同步。在线文档见 `http://127.0.0.1:8000/docs`。

## 一、通用约定

| 项目 | 约定 |
| --- | --- |
| 前缀 | `/api/v1` |
| 响应体 | `{code, message, data}`，`code = 0` 表示成功 |
| 学生端鉴权 | 请求头 `Authorization: Bearer <token>`（可选登录，游客可用大部分功能） |
| 管理端鉴权 | 请求头 `X-Admin-Token`（登录接口签发，或 `.env` 中的固定 Token） |
| 编码 | UTF-8，请求体 `Content-Type: application/json; charset=utf-8` |

## 二、错误码

| 错误码 | 含义 | 说明 |
| --- | --- | --- |
| 0 | 成功 | 业务处理成功，data 为返回数据 |
| 4000 | 参数错误 | 入参缺失、格式错误或超出范围 |
| 4001 | 文档解析失败 | 知识库只接受 txt / md / csv / pdf / docx，且内容不能为空 |
| 4002 | 内容不合规 | 出题主题命中面向未成年人的内容安全词表 |
| 4003 | 错题本为空 | 错题本没有内容时调用「只练错题」 |
| 4010 | 未登录 | 学生端接口需要登录后才能访问（错题本、个人中心） |
| 4011 | 管理端未认证 | 缺少或使用了无效的 X-Admin-Token |
| 5000 | 服务器内部错误 | 兜底异常，对外只给统一提示，细节留在服务端日志 |

## 三、学生端接口（16 个）

| 序号 | 方法 | 路径 | 功能 | 参数 |
| --- | --- | --- | --- | --- |
| 1 | GET | `/api/v1/grades` | 学段选项拉取 | — |
| 2 | GET | `/api/v1/health` | 服务健康检查 | — |
| 3 | GET | `/api/v1/knowledge/documents` | 知识库文档列表 | — |
| 4 | POST | `/api/v1/knowledge/documents` | 上传知识库文档 | file: UploadFile（可选） |
| 5 | DELETE | `/api/v1/knowledge/documents/{doc_id}` | 删除知识库文档 | doc_id: str |
| 6 | POST | `/api/v1/quiz/generate` | 出题（支持检索增强） | req: QuizGenerateRequest |
| 7 | POST | `/api/v1/quiz/submit` | 答题提交与判题结算 | req: AnswerSubmitRequest |
| 8 | POST | `/api/v1/report/generate` | 生成复盘报告 | req: ReportGenerateRequest |
| 9 | POST | `/api/v1/user/login` | 用户登录 | req: LoginRequest |
| 10 | GET | `/api/v1/user/profile` | 个人中心数据 | — |
| 11 | POST | `/api/v1/wrong/practice` | 只练错题组卷 | req: WrongPracticeRequest |
| 12 | GET | `/api/v1/wrong/questions` | 错题本总览 | — |
| 13 | PUT | `/api/v1/wrong/questions` | 修改单条错题 | req: WrongItemRequest |
| 14 | DELETE | `/api/v1/wrong/questions` | 清空错题本 | — |
| 15 | DELETE | `/api/v1/wrong/questions/item` | 删除单条错题（请求体传题干） | req: WrongItemRequest |
| 16 | DELETE | `/api/v1/wrong/questions/{stem}` | 删除单条错题（路径传题干） | stem: str |

## 四、管理端接口（13 个）

| 序号 | 方法 | 路径 | 功能 | 参数 |
| --- | --- | --- | --- | --- |
| 17 | GET | `/api/v1/admin/dashboard` | 运行看板统计 | — |
| 18 | POST | `/api/v1/admin/login` | 管理端登录 | req: AdminLoginRequest |
| 19 | GET | `/api/v1/admin/logs` | 操作日志查询 | limit: int（可选） |
| 20 | GET | `/api/v1/admin/me` | 校验管理端登录态 | — |
| 21 | GET | `/api/v1/admin/questions` | 题库资源池查询 | theme: Union（可选）、grade: Union（可选）、keyword: Union（可选）、page: int（可选）、size: int（可选） |
| 22 | POST | `/api/v1/admin/questions` | 新增题目 | req: QuestionPoolRequest |
| 23 | GET | `/api/v1/admin/questions/themes` | 题库主题分布 | — |
| 24 | PUT | `/api/v1/admin/questions/{qid}` | 修改题目 | qid: int、req: QuestionPoolRequest |
| 25 | DELETE | `/api/v1/admin/questions/{qid}` | 删除题目 | qid: int |
| 26 | GET | `/api/v1/admin/sessions` | 闯关记录查询 | page: int（可选）、size: int（可选）、grade: Union（可选）、source: Union（可选） |
| 27 | GET | `/api/v1/admin/trend` | 近 N 天闯关量趋势 | days: int（可选） |
| 28 | GET | `/api/v1/admin/users` | 用户分页查询 | keyword: Union（可选）、page: int（可选）、size: int（可选） |
| 29 | POST | `/api/v1/admin/users/{user_id}/status` | 启用 / 停用用户 | status: int（可选） |

> 合计 **29** 个接口（学生端 16 + 管理端 13）。
