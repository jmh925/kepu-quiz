# 接口清单（由源码自动生成）

> 本文档由 `tools/gen_docs.py` 从 FastAPI 路由表导出，
> 修改接口后请重跑脚本以保持同步。在线文档见 `http://127.0.0.1:8000/docs`。

## 一、通用约定

| 项目 | 约定 |
| --- | --- |
| 前缀 | `/api/v1` |
| 响应体 | `{code, message, data}`，`code = 0` 表示成功 |
| 学生端鉴权 | 请求头 `Authorization: Bearer <token>`（注册登录、游客体验或小程序凭证登录后签发；出题与判题不强制携带） |
| 管理端鉴权 | 请求头 `X-Admin-Token`（登录接口签发，或 `.env` 中的固定 Token） |
| 编码 | UTF-8，请求体 `Content-Type: application/json; charset=utf-8` |

## 二、错误码

| 错误码 | 含义 | 说明 |
| --- | --- | --- |
| 0 | 成功 | 业务处理成功，data 为返回数据 |
| 4000 | 参数错误 | 入参缺失、格式错误或超出范围 |
| 4001 | 文档解析失败 | 知识库只接受 txt / md / csv / pdf / docx，且内容不能为空 |
| 4002 | 内容不合规 | 出题主题命中面向未成年人的内容安全词表 |
| 4003 | 错题本为空 | 错题本没有内容时调用「只练错题」 |
| 4010 | 未登录 | 学生端接口需要登录后才能访问（错题本、个人中心） |
| 4011 | 管理端未认证 | 缺少或使用了无效的 X-Admin-Token |
| 5000 | 服务器内部错误 | 兜底异常，对外只给统一提示，细节留在服务端日志 |

## 三、学生端接口（19 个）

| 序号 | 方法 | 路径 | 功能 | 参数 |
| --- | --- | --- | --- | --- |
| 1 | GET | `/api/v1/grades` | 学段选项拉取 | — |
| 2 | GET | `/api/v1/health` | 服务健康检查 | — |
| 3 | GET | `/api/v1/knowledge/documents` | 知识库文档列表 | — |
| 4 | POST | `/api/v1/knowledge/documents` | 上传知识库文档 | file: UploadFile（可选） |
| 5 | DELETE | `/api/v1/knowledge/documents/{doc_id}` | 删除知识库文档 | doc_id: str |
| 6 | POST | `/api/v1/quiz/generate` | 出题（支持检索增强） | req: QuizGenerateRequest |
| 7 | POST | `/api/v1/quiz/submit` | 答题提交与判题结算 | req: AnswerSubmitRequest |
| 8 | POST | `/api/v1/report/generate` | 生成复盘报告 | req: ReportGenerateRequest |
| 9 | POST | `/api/v1/user/guest` | 游客体验，建临时账号并签发令牌 | req: LoginRequest（可选） |
| 10 | POST | `/api/v1/user/login` | 账号口令登录或小程序凭证登录 | req: LoginRequest |
| 11 | POST | `/api/v1/user/merge-guest` | 把游客数据并入正式账号 | req: MergeGuestRequest、guest_token 可选 |
| 12 | GET | `/api/v1/user/profile` | 个人中心数据 | — |
| 13 | POST | `/api/v1/user/register` | 登录名与口令注册 | req: RegisterRequest |
| 14 | POST | `/api/v1/wrong/practice` | 只练错题组卷 | req: WrongPracticeRequest |
| 15 | GET | `/api/v1/wrong/questions` | 错题本总览 | — |
| 16 | PUT | `/api/v1/wrong/questions` | 修改单条错题 | req: WrongItemRequest |
| 17 | DELETE | `/api/v1/wrong/questions` | 清空错题本 | — |
| 18 | DELETE | `/api/v1/wrong/questions/item` | 删除单条错题（请求体传题干） | req: WrongItemRequest |
| 19 | DELETE | `/api/v1/wrong/questions/{stem}` | 删除单条错题（路径传题干） | stem: str |

## 四、管理端接口（14 个）

| 序号 | 方法 | 路径 | 功能 | 参数 |
| --- | --- | --- | --- | --- |
| 20 | GET | `/api/v1/admin/dashboard` | 运行看板统计 | — |
| 21 | POST | `/api/v1/admin/login` | 管理端登录 | req: AdminLoginRequest |
| 22 | GET | `/api/v1/admin/logs` | 操作日志查询 | limit: int（可选） |
| 23 | GET | `/api/v1/admin/me` | 校验管理端登录态 | — |
| 24 | GET | `/api/v1/admin/questions` | 题库资源池查询 | theme: Union（可选）、grade: Union（可选）、keyword: Union（可选）、page: int（可选）、size: int（可选） |
| 25 | POST | `/api/v1/admin/questions` | 新增题目 | req: QuestionPoolRequest |
| 26 | GET | `/api/v1/admin/questions/themes` | 题库主题分布 | — |
| 27 | PUT | `/api/v1/admin/questions/{qid}` | 修改题目 | qid: int、req: QuestionPoolRequest |
| 28 | DELETE | `/api/v1/admin/questions/{qid}` | 删除题目 | qid: int |
| 29 | GET | `/api/v1/admin/sessions` | 闯关记录查询 | page: int（可选）、size: int（可选）、grade: Union（可选）、source: Union（可选） |
| 30 | GET | `/api/v1/admin/trend` | 近 N 天闯关量趋势 | days: int（可选） |
| 31 | GET | `/api/v1/admin/users` | 用户分页查询（按登录名或昵称） | keyword: Union（可选）、page: int（可选）、size: int（可选） |
| 32 | GET | `/api/v1/admin/users/{user_id}` | 单个学生的答题信息分类（资料 / 统计 / 逐次闯关 / 薄弱知识点 / 错题明细 / 知识库资料） | user_id: int |
| 33 | POST | `/api/v1/admin/users/{user_id}/status` | 启用 / 停用用户 | status: int（可选） |

> 合计 **33** 个接口（学生端 19 + 管理端 14）。
